

def evaluate_source_score(queries_data,retrieved_data):

    # expected_source = []
    # for data in queries_data:
    #     expected_source.append(data["expected_score"])
    
    # retrieved_sources = []
    # for data in retrieved_data:
    #     retrieved_sources.append(data["source"])

    # query_score = []

    # for exp_source,ret_source in zip(expected_source,retrieved_sources):
    #     for source in ret_source:
    #         if(exp_source==source):

    return

